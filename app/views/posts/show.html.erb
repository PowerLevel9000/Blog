<div class="card border-info  m-3">
  <div class="card-header bg-transparent p-0"><%= render 'users/user_badge', user: @post.author %></div>
  <div class="card-body">
    <h5 class="card-title d-flex justify-content-between"><%= @post.title %><span><span>Comments 💬 :<%= @post.comments_counter %></span> <span>likes 💕 :<%=@post.likes_counter%></span></span></h5>
    <p class="card-text"><%= @post.text %></p>
  </div>
  <ul class="list-group list-group-flush">
    <% @post.comments.each do |comment| %>
      <li class="comments list-group-item"><%= comment.author.name%> : <%= comment.text %></li>
    <% end %>
  </ul>
  <div class="card-footer text-center bg-secondary fs-3 text-light">
    Thank you
  </div>
</div>
